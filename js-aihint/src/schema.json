{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "version": { "type": "string" },
    "type": { "type": "string" },
    "target": { "type": "string" },
    "issuer": { "type": "string" },
    "score": { "type": "number" },
    "method": { "type": "string" },
    "issued_at": { "type": "string" },
    "expires_at": { "type": "string" },
    "comment": { "type": ["string", "null"] },
    "public_key_url": { "type": "string" },
    "signature": { "type": ["string", "null"] }
  },
  "required": [
    "version", "type", "target", "issuer", "score", "method", "issued_at", "expires_at", "public_key_url"
  ]
} 